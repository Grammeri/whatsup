import React from 'react';

const Logout = () => {
    // Logic for handling logout

    return (
        <button onClick={handleLogout}>
            Logout
        </button>
    );
};

export default Logout;
